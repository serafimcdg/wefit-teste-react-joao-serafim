'use client';
import styled from 'styled-components';

export const FonteCaixaAltaButton = styled.span`
  font-family: var(--font-open-sans), system-ui, -apple-system, 'Segoe UI', Roboto, Arial, sans-serif;
  font-weight: 700;
  font-style: normal;
  font-size: 12px;
  line-height: 1;
  letter-spacing: 0;
  text-align: center;
  text-transform: uppercase; 
`;
export const QuantidadeCarrinhoButton = styled.span`
  font-family: var(--font-open-sans), system-ui, -apple-system, 'Segoe UI', Roboto, Arial, sans-serif;
  font-weight: 400;     
  font-style: normal;
  font-size: 12px;
  line-height: 1;        
  letter-spacing: 0;
  text-align: center;
  display: inline-flex;
  align-items: center;
`;